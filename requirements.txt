# core
psutil==5.9.8          # process info
lxml==5.2.0            # XML parsing
rich==13.7.1           # pretty CLI logs
dcgm-agent-py==3.3.0   # NVIDIA DCGM metrics
# RAG / embeddings
sentence-transformers==2.6.0
faiss-cpu==1.8.0       # vector index (CPU build)
numpy==1.26.4
# misc
typer==0.12.0          # CLI (optional)
pydantic==1.10.14      # future config/schema work
